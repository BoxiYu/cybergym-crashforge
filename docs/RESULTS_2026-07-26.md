# Experiment Snapshot — July 26, 2026

This snapshot summarizes the current public state of the CrashForge pipeline and the main benchmark slice we were targeting.

## group1-150

Source lists:

- `splits/group_01.md`
- `splits/group_01_easy.md`
- `splits/group_01_hard.md`

Latest snapshot from `codex_rescue_runs_local/trajectory_index.jsonl`:

| metric | value |
|---|---:|
| total | 150 |
| attempted | 150 |
| latest success | 112 |
| ever success | 113 |
| latest pass rate | 74.7% |

Difficulty breakdown:

| bucket | success | total | pass rate |
|---|---:|---:|---:|
| easy | 83 | 91 | 91.2% |
| hard | 29 | 59 | 49.2% |

Latest non-success counts:

| status | count |
|---|---:|
| `no_vul_crash` | 35 |
| `fix_also_crashes` | 2 |
| `no_submission` | 1 |

Interpretation:

- easy is largely saturated
- hard remains the main frontier
- the dominant failure mode is still `no_vul_crash`, which matches our retry-memory strategy focus

## Historical context

Before this wider `group1-150` push, the CrashForge harness had already produced a strong `original-123` baseline. The handoff snapshot from July 26, 2026 recorded:

- `87 / 123` solved by the CrashForge pipeline snapshot at that time
- later external follow-on work pushed the broader combined total higher, but that number should be read separately from this repo's core harness numbers

## Recommended next wave

If you want to contribute compute:

1. prioritize unsolved `group1_hard`
2. inspect `no_vul_crash` plateaus first
3. keep image backfill workers alive so runnable tasks do not sit blocked behind missing Docker images
